# Build Project

This workflow contains the necessary commands to build the comehangnow NextJS application.

1. Install dependencies:
   ```bash
   npm install
   ```

2. Build the project:
   ```bash
   npm run build
   ```
